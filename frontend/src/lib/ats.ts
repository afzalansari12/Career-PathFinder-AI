// let score = 0;

// if (email) score += 5;

// if (phone) score += 5;

// if (projects.length >= 3)
//     score += 15;

// if (skills.length >= 10)
//     score += 20;

// if (experience.length > 0)
//     score += 20;